# /etc/default/rover-auto-update
# Extra flags passed to: ogun update <args>
#
# Default behavior (recommended): update rover_server only.
# Do NOT set --flash-teensy for unattended updates unless you
# explicitly want periodic MCU reflashes.

OGUN_UPDATE_ARGS=""
